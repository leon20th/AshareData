# 只声明下限、不设上限：pip 会按当前解释器自动选兼容的最新版，
# 因此不要求用户为这个库固定 Python 版本（Python 3.9+ 均可安装）。
# 下限是「API 可用」的最低版本，不是锁版本；需要复现精确环境时用 requirements.lock 或 pip freeze。
baostock>=0.8.9
beautifulsoup4>=4.12
lxml>=5.0
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
pillow>=10.0
polars>=1.0
pyarrow>=15.0
pypinyin>=0.51
requests>=2.31
selenium>=4.20
torch>=2.0
torchvision>=0.15
tqdm>=4.66
